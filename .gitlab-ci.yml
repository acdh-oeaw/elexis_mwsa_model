image: "python:3.7.7-buster"
stages:
  - test
  - train
  - deploy

test:
  stage: test
  script:
    - pip install -r requirements.txt
    - python nltk_install.py
    - pytest -o log_cli=true  test/
dvc:
  stage: train
  script:
    - pip install dvc==1.0.2
    - pip install -r requirements.txt
    - python nltk_install.py
    - dvc repro
  artifacts:
    paths:
      - mwsa_model/output/models/de.pkl
      - mwsa_model/output/models/en.pkl
      - mwsa_model/output/models/en.joblib
    expire_in: 4 weeks
deploy:
  image: google/cloud-sdk
  stage: deploy
  script:
    - export
    - echo $GOOGLE_SERVICE_ACCOUNT_FILE
    - ls /tmp
    - gcloud auth activate-service-account --key-file=$GOOGLE_SERVICE_ACCOUNT_FILE
    - gsutil cp mwsa_model/output/models/* gs://elexis_mwsa_models


#staging:
#  stage: deploy
#  script:
#    - apt-get update -qy
#    - apt-get install -y ruby-dev
#    - gem install dpl
#    - dpl --provider=heroku --app=gitlab-ci-python-test-staging --api-key=$HEROKU_STAGING_API_KEY
#  only:
#    - master
